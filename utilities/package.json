{
  "name": "playlist-generaetor-utilities",
  "version": "1.0.2",
  "main": "src/index.ts",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
    "start": "nodemon --watch src --ext ts --exec \"npx tsc --declaration\"",
    "build": "npx tsc --declaration"
  }
}
