{
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "scripts": {
    "start": "concurrently -n frontend,backend -c \"bgBlue.bold,bgMagenta.bold\" \"cd frontend && yarn start\" \"cd backend && yarn start\"",
    "tunnel": "ngrok start --all --config=ngrok.yml"
  }
}