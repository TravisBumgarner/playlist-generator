{
  "workspaces": ["utilities", "backend", "frontend"],
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently -n frontend,backend,utilities -c \"bgBlue.bold,bgMagenta.bold,bgGreen.bold\" \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"npm start --workspace=utilities\"",
    "deploy:nfs": "concurrently -n backend,frontend -c \"bgMagenta.bold,bgBlue.bold\" \"npm run deploy:nfs --workspace=backend\" \"npm run deploy:nfs --workspace=frontend\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13"
  }
}
